---
- name: "output group info"
  debug:
    var: groups['virtual-kubelet']

- name: "output group info"
  debug:
    var: hostvars['host1']

- name: "output group info"
  debug:
    var: stress_vk_per_host

- name: "output group info"
  debug:
    var: default_vk_per_host
