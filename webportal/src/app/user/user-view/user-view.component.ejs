<section class="content-header"><%= breadcrumb({ breadcrumbTitle: "User Management" }) %></section>

<section class="content">
  <div class="box">
    <div id="modalPlaceHolder" class="box-body" />
    <button class="btn btn-success btn-lg add-user-btn" id="toRegister" onclick="redirectToAddUser()">Add User</button>
    <div id="view-table" class="box-body">
      <%= userTable({ userLists: [] }) %>
    </div>
  </div>
  <%= loading() %>
</section>
